import { ValueChangeFlashDirective } from './value-change-flash.directive';

describe('ValueChangeFlashDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueChangeFlashDirective();
    expect(directive).toBeTruthy();
  });
});
